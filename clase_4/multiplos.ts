//entre 1 y 100
//indicar por pantalla los numeros que son multiplos de 2 y de 3,
// si es de ambos mostrar una sola vez.

//contador mod 2 == 0 o contador mod 3 == 0

for (let contador : number = 1 ; contador < 100; contador++) {
    if (contador % 2 == 0 || contador % 3 == 0) {console.log(contador)
        
    } else {
        
    }
    
}